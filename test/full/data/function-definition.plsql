function a(
 p_ab varchar2,
 p_abc varchar2,
 p_ad number ,
 p_adddd varchar2,
 p_a char ,
 p_a varchar2,
 p_a varchar2,
 p_a varchar2,
 p_a varchar2,
 p_a varchar2,
 p_a varchar2,
 p_b boolean
)
return number
is
 a number;
 ab number;
 abc number;
 abcd number;
begin
 1 + 2;
 if 1 = 3 and 4 = 5 then
 null;
 end if;

 select 1 as b, sysdate, to_char( to_char( 3 + 1)) as d, asd, fas, df as asdf, asdfasd, asdf, asdfasdf, asdfasdf, asdfasdf, asdfasdf, asdfasdf into b, c, d.y from c join d on c.a = d join e on c.e.e.y = y where 1 = 2;
 FOR c IN (SELECT 1 AS a 
 FROM DUAL) LOOP
 htp.p(p.a);
 END LOOP;
END ;
